module.exports = (data) => {
  const html = `
    <!-- Contact start -->
    <!-- Contact end -->
    `;

  const css = `
    /* Contact start */
    /* Contact end */
    `;

  const javaScript = `
    // Contact start
    // Contact end
    `;

  return { html: html.trim(), css: css.trim(), javascript: javaScript.trim() };
};
